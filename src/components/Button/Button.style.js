import { StyleSheet } from 'react-native'

export default StyleSheet.create({
    container: {
        backgroundColor:"blue",
        padding:10,
        margin:10,
        borderWidth:1,
        borderColor:"balck",
        borderRadius:8,
    },
    title:{
        color:"white",
    }

})